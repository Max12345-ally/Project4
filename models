const mongoose = require('../db/connection')
const Schema = mongoose.Schema

const Assest = new Schema({
    firstName = String,
    lastName = String,
    comments = String


})

module.exports = mongoose.model('Topic', Topic)